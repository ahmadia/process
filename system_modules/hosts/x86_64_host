#%Module
# Modulefile automagically loaded when on x86_64 hosts (shaheen.hpc.kaust.edu.sa)
#
# KAUST Supercomputing Laboratories
# shaheen-help@kaust.edu.sa

module-whatis   "module automatically loaded when compiling on the x86_64 front-end node"

# only x86_64 compilers are available on x86_64 platforms
conflict ppc64_target

# sanity
conflict ppc64_host

proc ModulesHelp { } {
puts stderr \
"This module is automatically loaded when you are on an x86_64
front-end node.  If you are getting conflict errors against this
module, you should probably be compiling from a ppc64 front-end
node (e.g. shaheen.hpc.kaust.edu.sa).

In particular, any code you intend to run on the BlueGene/P portion
needs to be cross-compiled from a ppc64 front-end node.
"
}

# The below magic is for Emacs editing convenience
# Local Variables: 
# mode: tcl
# End:
#

